pip install Pillow==10.1.0

pip install Django==4.1
pip install Jinja2==3.1.2
pip install zipp==3.17.0
pip install requests==2.31.0

pip install opencv-python==4.7.0.72
pip install opencv-contrib-python==4.7.0.72

pip install celery==5.3.6
pip install celery-progress==0.3
pip install django-celery-results==2.5.1
pip install redis==5.0.1

ejecutar celery
celery -A servSegurityCam  worker -l info

obtener una URL de estado de la cola 
http://127.0.0.1:8000/celery-progress/f2cf777a-ca54-47ec-8b08-12ecf7919752/

config redis https://redis.io/commands/acl-cat/
para entrar a docker es :
    docker exec -it jovial_cerf bash
para entrar a REDIS se escribe :
    redis-cli
para saber los usuarios de redis :
    ACL USERS
    ACL USERS
para cambiar clave del usuario :
    AUTH [username] password
    AUTH <password>
or
    CONFIG SET requirepass "mypass"
    AUTH mypass


